# PhiChart Editor 项目说明

> 这份文档用于帮助接手此项目的编程者、AI 迅速了解此项目。
>
> **阅读顺序**：本文（总览 + 单位 + 坑）→ `docs/01-官方格式规格.md` → `docs/02-RPE格式规格.md` → `docs/03-渲染与数学.md` → `docs/04-参考资料与工具.md` → `docs/05-渲染器实现.md`（已实现的渲染器 v1）。
>
> **本文档的可信度标记**（全文通用）：
> - `[实测]` 已用本仓库 `packages/` 下的两个样本包或 `assets/` 资源验证过（脚本见 `tools/`，结论见 `tools/deep-findings.txt`）。
> - `[文档]` 来自参考文档，附链接（见 `docs/04-参考资料与工具.md`）。
> - `[未验证]` 暂无可靠来源，仅为推测或社区说法，**实现前需要再确认**。
> - 不要凭印象填写单位与字段名；本文件里每个数字都应能追溯到上面三类之一。

---

## 1. 项目是什么

Phigros 是一款知名 2D 下落式音乐游戏。在游戏关卡中，存在多根判定线（line），音符（note）从远处靠近判定线，最终与判定线重合，玩家需要在重合的瞬间做出对应操作。

我们把关卡内容称为**谱面（chart）**，设计关卡称为**制谱**。

本项目要做两件事：

1. **游戏渲染器**：渲染出关卡模拟，自动游玩关卡（自动击打），用于预览最终效果——note 落到判定线即视为被击中，播放特效并计分。
2. **制谱器（编辑器）**：用于设计谱面，内置实时渲染关卡的功能。UI 风格参考 Adobe Photoshop 等：暗色背景、扁平图标、多彩且饱和度适当的配色。

同时必须支持读取两种谱面格式：**official 格式**（游戏本体格式）与 **RPE 格式**（Re:PhiEdit 编辑器格式）。

### 1.1 硬性需求

- 选用音频播放器时应注意：需要允许精准跳转到某一刻，需要允许暂停、播放，需要允许倍速播放。
- 选用 UI 库时应注意：高性能，渲染器需要保持 60–90 帧，多线与 note 贴图的同屏半透明高性能渲染。制谱器需考虑同屏渲染数千个 event 的优化。未来预留 3D/伪 3D 改造的可能性。
- 选用框架时注意：注重可分发性，为网页或其他便于多平台打包/编译，优先考虑 Windows PC 端。

### 1.2 同类实现（可参考/可对照的开源实现）

| 项目 | 语言/技术 | 用途 | 对本项目的价值 |
| --- | --- | --- | --- |
| [Phira / prpr](https://github.com/TeamFlos/phira) | Rust + macroquad | 社区播放器，支持 official / RPE / PEC / PBC | 单位换算、速度、层叠加、缓动的**权威实现参考**（`prpr/src/parse/*.rs`）`[文档]` |
| [Phichain](https://phichain.rs/) | Rust | Phigros 制谱器 + 转谱器 | 制谱器交互设计、**RPE 速度单位定义**、格式能力对比（见 `docs/04` R10）`[文档]` |
| [sim-phi](https://github.com/lchzh3473/sim-phi) | TypeScript / Canvas | 官方格式模拟器（参考实现） | 官方渲染的**具体常数**：线贴图尺寸、淡出时间、判定区宽、特效配色（见 `docs/03` §8）`[文档]` |
| Lchzh 模拟器 / docs | Web/TS | 官方格式研究 | 官方格式与实测数据来源（Lchzh Docs）`[文档]` |

### 1.3 当前实现状态（渲染器 v1）

已实现一个**只渲染关卡本体**（无开场/结束动画）的第一版渲染器，代码在 `src/`，详细说明见 **`docs/05-渲染器实现.md`**：

- 内核：`src/core/` —— 内部统一模型 + 官方/RPE 两个解析器 + 事件/高度求值 + 自动游玩判定与计分；无第三方依赖。
- 渲染：`src/render/canvas2d.js`（Canvas2D 后端，背景模糊/判定线/音符/Hold/42 帧打击特效）。
- 应用：`src/app/` + `index.html`（包加载、播放器、HUD、快捷键）。
- 运行：项目根目录起静态服务器（`python -m http.server 8099`）后打开 `index.html`；也可用「选择谱面包目录 / zip」加载。
- 测试：`node tools/render-tests.mjs`（71 项）、`node tools/render-smoke.mjs`（29 项，含 Hold 绘制几何）、`node tools/app-smoke.mjs`（23 项，应用层接线）、`node tools/http-smoke.mjs`（27 项），当前全部通过。
- 范围：官方格式功能完整（formatVersion 1/3/2/3473、四类事件、四类音符）；RPE 只做「核心 + 事件层 + 缓动 + 音符属性 + 父子线（按 Phira 口径）+ 自定义线材质」，**扩展事件 / Control / attachUI / GIF / hitsound 暂不实现**。
- 可调项：音符宽度默认 **W/8**（`N`/`M` 调整）。

---

## 2. 判定线（JudgeLine）与事件（Event）

- 通常会有一根或多根主判定线上有音符落下，其他判定线起表演作用。`[文档]` 官谱惯例是 24 根判定线；判定线数目超过 100 会导致游戏异常（音符不再垂直移动、动画中断）。
- 判定线的中心位置（x/y）、旋转角度、透明度、音符下落速度**均随时间变化**，通过事件（event）实现。
- 事件通常连续：事件按 `startTime` 从小到大排列，每个事件的 `startTime` 等于上一个事件的 `endTime`；第一条事件（除速度事件）的 `startTime` 是一个足够小的哨兵值，最后一条事件的 `endTime` 是一个足够大的哨兵值。`[文档]`
- 事件数量巨大（本项目 RPE 样本有 8.1 万条事件），必须做到「给定时刻 O(log n) 求值」：预先把每条事件编译成关键帧数组 + 二分查找（不要让每帧遍历所有事件）。

---

## 3. 文件与包（package）

一个 Phigros 关卡存储为一个包，包内应至少包含：

- **背景图**（曲绘，illustration/background）：任意格式的图片。
- **背景音乐**（音频，audio/song）：任意格式的音频。
- **谱面文件**（chart）：一个 json 文件，通常非常大（本仓库样本 25.8 MB / 40.8 MB）。
- 还可能包含一个 **txt 文档**记录关卡元数据（可选，格式未统一；也可能是 GBK 编码）。
- RPE 格式允许自定义资源（纹理、打击音等），也一并放在包内根目录下（可选）。

包也可能以 zip 压缩包形式提供，其内部结构应与上述一致。

**包内文件引用的解析规则** `[文档]`：`Texture` / `song` / `background` / `hitsound` 都是**相对于谱面文件所在目录**的路径；文件名可能包含空格与 `#`（本项目样本就有 `Chart_AT #3649.json`、`Illustration #4286.png`、`music #1988.wav`），解析时不要对文件名做假设。

### 3.1 本仓库的两个调试包 `[实测]`

| 包 | 谱面 | 音频 | 曲绘 | 元数据 |
| --- | --- | --- | --- | --- |
| `packages/白复生 AT（official格式）` | `Chart_AT #3649.json` 25.8 MB，formatVersion 3，174 BPM，24 线，1156 note | `music #1988.wav` 31.1 MB，48 kHz/16 bit/立体声，161.72 s | `Illustration #4286.png` 2.45 MB | 无（需手动填写元数据） |
| `packages/领土战争AT（RPE格式）` | `29519800.json` 40.8 MB，RPEVersion 140，140 BPM，24 线，1417 note、81320 条事件 | `29519800.wav` 26.6 MB，44.1 kHz/16 bit/立体声，150.86 s | `29519800.png` 1.97 MB | `info.txt` 222 B（UTF-8，字段 `#/Name/Path/Song/Picture/Chart/Level/Composer/Charter/Illustrator`） |

注意：官方格式的包**不带元数据**（曲名、曲师、谱师、难度）。编辑器必须允许手动填写与保存元数据；RPE 的 `META` 字段才是完整元数据的来源。

### 3.2 `assets/` 资源 `[实测]`

| 文件 | 尺寸 | 形状 | 实测主色 |
| --- | --- | --- | --- |
| `Tap.png` / `TapHL.png` | 989×100 / 1089×200 | 单根长条，两端斜切；HL 版更大，含白色描边、黄色外发光与两端双箭头 | 主体 `#0AC3FF`，描边 `#FFFFFF`；HL 另加 `#FFFF66` |
| `Drag.png` / `DragHL.png` | 989×60 / 1089×160 | 同 Tap，但更薄（高度约为 Tap 的 0.6） | 主体 `#F0ED69`，描边 `#FFFFFF` |
| `Flick.png` / `FlickHL.png` | 989×200 / 1089×300 | **分为左右两半、中间留缝**，缝的两侧各有一个小三角标记（中间区域有白色图元） | 主体 `#FE4365`，描边 `#FFFFFF`；HL 另加 `#FFFF66` |
| `Hold.png` / `HoldHL.png` | 989×2000 / 1062×2048 | 一根整体的渐变长条（**不是**「尾/中/头」三段式贴图）；两端各约 1–2% 收窄形成卡口（实测卡口高约 40px）；整图**半透明**，逐行实测为「尾部 白+alpha101 → 头部 青+alpha241」的平滑渐变。HL 的本体为 964×1950（位于 (49,49)），左右与下端各有约 40px 光效外扩（**不能算进长条长度**）。<br>渲染按 `capPx` 源像素取两端卡口（渲染约 6.5px）、其余整根拉伸（`H` 键可切换其它取样）；双押与普通走同一套分段逻辑，差别只是 HL 贴图本体多出的光效。⚠️ 若换成别的资源包（如 `[48 光效][48 帽][主体][48 帽][48 光效]`），渲染器会先校验尺寸再自动识别分段，也可用 `?holdCap=48&holdGlow=48` 显式指定 | 渐变 `#9CE9FF` → `#FFFFFF`；头部实测 `156,233,255` |
| `hit.png` | 2520×2160 | 打击特效图集，**7 列 × 6 行 = 42 帧**，每帧 360×360，透明底 + 白色图形 | `#F9F9F9` / `#CBCBCB`（带 alpha） |
| `click.wav` / `drag.wav` / `flick.wav` | — | 打击音效，时长 0.098 s / 0.445 s / 0.390 s，44.1 kHz 16 bit 立体声 | — |

> 结论与实现要点 `[实测]`：
> - **音符贴图自带颜色**（Tap 蓝 / Drag 黄 / Flick 红 / Hold 浅蓝），渲染时不要再按线颜色给音符上色；只有 `hit.png` 是白色图形，需要用判定线颜色（金色 `#feffa9`）相乘着色。
> - `hit.png` 的 7×6 分帧由图像内容确认；按「帧索引 = 进度 × 42」取子矩形。
> - Hold 需要把整根贴图拉伸到音符长度，最好保留两端卡口（可视为 9-slice：两端各裁一小段不拉伸）。Phira 的做法是另配 `holdAtlas`（尾部/头部像素高度）+ 可选重复式拉伸。
> - `[文档]`：`HL` 贴图的触发条件由参考实现确认——**同一时刻存在 ≥ 2 个音符（按秒精确比较）且开启「多押提示」时使用**（对应 Phira 资源包中的 `*_mh`）。
> - 颜色取自像素采样（脚本 `tools/sample-colors.mjs`），可作为渲染器的配色基准。

### 3.3 参考效果图

仓库根目录的 `渲染效果图.png`（1504×978）是一张渲染结果参考图 `[实测]`，可与本文规格逐项对照：

- 背景为高斯模糊 + 压暗的曲绘；
- 判定线为细金线，**横向贯穿整个画面并延伸到屏幕外**（与「线长 5.76H」相符，而不是只有屏幕宽度）；
- Tap 音符为蓝色条 + 白色描边 + 两端尖角箭头 + 淡黄外发光 —— 与 `TapHL.png`（高亮/多押皮肤）一致，实现时注意「普通」与「HL」两套贴图的差别；
- 命中特效为金色的方块/圆环/旋涡图形，与 `hit.png` 的 42 帧内容一致（金 = 用 `rgba(255,236,160,0.882)` 着色）。

---

## 4. 单位制与时间（**最容易出错的地方，先看这张表**）

两套格式采用了**完全不同的单位制、字段名与枚举编号**。下表是最小必记集合：

| 项目 | official 格式 | RPE 格式 |
| --- | --- | --- |
| 时间单位 | `T` = 1/32 拍 = 128 分音符；秒 = `time × 1.875 / bpm` `[文档][实测]` | `beat` = `[整数, 分子, 分母]`，值 = `b[0] + b[1]/b[2]`；秒 = `beat × 60 / bpm`（多 BPM 分段计算）`[文档]` |
| BPM | 每条判定线各带一个 `bpm`，全曲恒定 | 根节点 `BPMList` 全局变速 + 每条线 `bpmfactor`（线当前 BPM = 全局 BPM / bpmfactor）`[文档]` |
| X 距离单位 | `1 X = 0.05625 W`（1920×1080 下 108 px），用于 `Note.positionX` `[文档][实测]` | `1 单位 = W / 1350`，坐标锚点在屏幕中心，x ∈ [−675, 675] `[文档][实测]` |
| Y 距离单位 | `1 Y = 0.6 H`（648 px），用于 `Note.floorPosition`、速度事件 `[文档][实测]` | `1 单位 = H / 900`，y ∈ [−450, 450] `[文档][实测]` |
| 线坐标（move 事件） | formatVersion 3：左下角为原点、右上角 (1,1)，`start/end` = x，`start2/end2` = y `[文档][实测]` | 屏幕中心为原点，x ∈ [−675,675]、y ∈ [−450,450] `[文档][实测]` |
| 旋转方向 | **逆时针为正**（角度制）`[文档]` | **顺时针为正**（Phira 转换时乘 −1）`[文档]` |
| 速度单位 | `Y/s`，1.0 = 常规速度 `[文档][实测]` | 1 = 120 RPE长度单位/秒 = 2/15 屏幕高度/秒（⇒ 官方 Y/s = RPE × 2/9）；见 §4.1 `[文档][实测]` |
| 不透明度 | `disappear` 事件，0–1 浮点 | `alpha` 事件 0–255 整数；note `alpha` 0–255；线的 `alphaControl` 用 0–1 |
| note 类型编号 | 1 Tap / 2 **Drag** / 3 **Hold** / 4 Flick `[文档][实测]` | 1 Tap / 2 **Hold** / 3 **Flick** / 4 **Drag** `[文档][实测]` |
| 偏移 `offset` | 单位**秒**（浮点） | `META.offset` 单位**毫秒**（整数） |
| 上下方向 | 用两个数组 `notesAbove` / `notesBelow` 表示 | note 自带 `above`（1 = 正面，其他 = 背面） |

> ⚠️ **四个必踩的坑**：① note 类型编号在两套格式里不同（Hold 是 official 的 3、RPE 的 2）；② 旋转方向相反；③ `offset` 单位不同（秒 vs 毫秒）；④ 速度值单位不同。写解析器时务必让内部数据模型与格式解耦，只在 parse/serialize 层做换算。

### 4.1 速度值的换算

- official：速度事件 `value` 的单位是 **Y/s**（即速度为 1 时，音符以 0.6 个屏幕高度/秒靠近判定线）`[文档][实测]`。
- RPE：**1 的速度 = 音符每秒下落 120 个单位，而画布高度为 900 单位，即每秒 2/15 屏幕高度** `[文档]`（Phichain 文档《速度》，且 Phichain 的 RPE 导入器原样搬运速度值，故该单位即 RPE 的单位）。换算：

  ```
  1 RPE 速度 = 120 RPE长度单位/s = (2/15) × H/s = (2/15) / 0.6 Y/s = 2/9 Y/s ≈ 0.2222 Y/s
  ⇒ 官方等效速度(Y/s) = RPE速度 × 2/9        ⇒ RPE速度 = 官方速度 × 4.5
  ```

  `[实测]` 与样本完全吻合：官方样本的「瞬移」是 `999`，RPE 样本的同类手法是 `4495.5 = 999 × 4.5`，`0.001 × 4.5 = 0.0045`。Phira 的 `SPEED_RATIO = (10/45)/HEIGHT_RATIO` 中的 `10/45` 正是 `2/9`，但它额外除以 `HEIGHT_RATIO = 0.83175`（换算到 prpr 自己的画布单位），结果约 `0.267`，比 `2/9` 快约 20%。**建议：内部统一用 Y/s，导入 RPE 时用 `2/9`（与官方/Phichain 语义一致），并保留一个可配置系数以便与 Phira 播放逐帧对齐。**

---

## 5. 渲染器的核心数学（完整推导见 `docs/03-渲染与数学.md`）

official 格式的判定线「垂直位置」定义为速度事件对**秒**的积分（记 `PJ(t)`，单位 Y）：

```
speedEvents 第 k 条：startTime = tk, value = vk
PJ(t) = Σ（t 之前所有事件的 v · Δ秒）+ vk · (t − tk) × 1.875 / bpm      （t 落在第 k 条事件内）
VJ(t) = vk                                                              （判定线实时速度，单位 Y/s）
```

`[文档]`（Lchzh Docs《相关计算》）`[实测]`：官方样本第 1 条判定线 280 个 note 的 `floorPosition` **全部**等于 `PJ(note.time)`（误差 < 1e-3），包括速度为 1/1.65/2.2/999 的 note——即

```
Note.floorPosition = PJ(Note.time)        （与 note 自身的 speed 无关）
```

音符在 `t` 时刻距判定线的垂直距离（单位 Y）：

```
非 Hold 音符：       Y(t) = η · (pN − PJ(t))
Hold 头部（t ≤ tN）：Y(t) = pN − PJ(t)
Hold 尾部：          YT(t) = Y(t) + η · tH × 1.875 / bpm
```

其中 `η` = `Note.speed`，`tH` = `Note.holdTime`，`pN` = `Note.floorPosition`。`[文档]`

配合 §4 的单位换算，即可得到屏幕坐标：

```
屏幕 x = 判定线中心 x + (positionX × 0.05625 W) 沿判定线方向
屏幕 y = 判定线中心 y + Y(t) × 0.6 H            沿判定线法线方向
（再叠加判定线的旋转与父线变换）
```

RPE 侧是同一套物理：把「速度事件积分」写作 floorPosition，按 §4.1 换算速度，按 `1 单位 = W/1350`、`H/900` 换算坐标。

---

## 6. 官方观感规格（渲染器目标）

| 元素 | 规格 | 来源 |
| --- | --- | --- |
| 背景图 | 缩放铺满窗口，亮度降到 0.4 倍（= 压暗 0.6），高斯模糊半径约 200–500 px | 项目原始说明；Phira 默认 `backgroundDim = 0.6`、`aspectRatio = 16/9` `[文档]` |
| 画布 | `画布宽 = min(实际宽, 实际高 × 16/9)`，高度取实际高（超宽屏加黑边） | lchzh 模拟器 `[文档]` |
| 判定线 | 贴图 **6220.8 × 7.68 px**（1920×1080 参考），缩放基准 `lineScale = H / 18.75`；即长度 **5.76 H**、宽度约 **0.0071 H**；颜色金色 `#feffa9`（AP）、`#a2eeff`（FC）、普通为白；多条线叠加的不透明度公式 `a = a1 + a2 − a1a2` | Lchzh Docs《实测数据》+ lchzh 模拟器源码 `[文档]` |
| note | 位置 = 判定线中心 + `positionX × 0.05625 W`（沿线方向）与 `Y(t) × 0.6 H`（法线方向），随线旋转；过线后 **0.16 s** 内淡出；`*HL` 贴图用于「同一时刻 ≥ 2 个音符且开启多押提示」时 | lchzh 模拟器 `[文档]` |
| 判定区域宽度 | `0.118125 × 画布宽`（= 2.1 X） | lchzh 模拟器 `[文档]` |
| Hold | 有厚度/长度，头部先接触判定线并固定在线上、循环播放击中动画，尾部继续靠近直到重合 | 项目原始说明 |
| 击中动画 | `hit.png` 42 帧（7×6），透明底白色图形，着色为 Perfect `rgba(255,236,160,0.882)`（≈`#FFECA0`）/ Good `rgba(180,225,255,0.922)`；本项目原稿写的 (255,243,163) = `#FFF3A3` 与之接近但不同。⚠️「42 帧」只对**本项目 `assets/hit.png`** 成立：参考实现把帧网格做成资源包参数（Phira `hitFx: [列, 行]`，文档示例 [5,6]、默认包 [8,7]），sim-phi 则按贴图短边自动切帧——`[未验证]` 官方帧数 | `[实测]` + lchzh 模拟器 `[文档]` |
| 音乐结束 | 音乐进度到 `音乐时长 − 0.22049 s` 时停止并结算；进度条按音乐时长移动 | Lchzh Docs《实测数据》`[文档]` |
| 分数 UI | 右上角分数（满分 1000000，显示为 `0` + 定点小数形式，如 7812.5 → `0007813`），左下角歌名，右下角难度 | Lchzh Docs《实测数据》`[文档]` |

**note 可见性（官方 v2.3.1 实测）** `[文档]`：

- `currentFloorPosition < −0.001` 且未打击 → 不渲染（浮点误差规则很微妙，见原文实现）。
- `speed × currentFloorPosition > 3.3333336`（即 Y > 2 H）→ 不可见（v2.0.0+）。
- 长度为 0 的 Hold 不渲染（`speed = 0` 或 `holdTime` 取整后为 0）。

---

## 7. 计分与判定（自动游玩/预览必须实现）

- 满分 1,000,000 = **判定分 900,000 + 连击分 100,000**；三个独立实现给出同一闭式：`score = round((900000·Perfect + 585000·Good + 100000·maxCombo) / N)`，其中 `585000 = 900000 × 0.65`。`[文档]`（萌娘百科 Phigros + PhiZone/player、sim-phi、Phira/prpr 三处实现一致）
- 物量 `N` = 谱面**非假音符**数量：**Drag 计入**（过线自动 Perfect，对 Acc 贡献恒为 1），Hold 记 1 个。`[文档]`（RPE 编辑器写出的 `numOfNotes` 不含 Hold，属编辑器行为，不参与官方计分）
- 单个 note 的判定分上限 = `900000 / 谱面物量`；Perfect 得 100%，Good 得 65%，其余 0。`[文档]`
- 连击分 = `最大连击数 / 物量 × 100000`（就近取整）；最大连击数的上限 = 物量。`[文档]`
- 准确率 ≈ 判定分 / 900000。`[文档]`

> 项目原始说明里的「每个 note 的分数为 1000000 / note 总数」是简化说法；实际是 90 万判定分均分 + 10 万连击分按最大连击比例给分。

**判定窗口** `[文档]`：

| 判定 | Tap / Hold | Drag | Flick | 效果 |
| --- | --- | --- | --- | --- |
| Perfect | ±80 ms | ±100 ms | ±140 ms | 金色特效，满分 |
| Good | ±80–180 ms | 无 | 无 | 蓝色特效，65% |
| Bad | ±180–220 ms（Hold 无 Bad） | 无 | 无 | 断连，note 变暗红消失 |
| Miss | 未击中 | 未击中 | 未击中 | 断连 |

补充规则 `[文档]`：Drag 需在重合时有手指位于判定区域内，Flick 需向任意方向滑动（一根手指一个方向只判定一次）；Hold 可提前松手、可换手，不影响判定，但已判 Miss 的 Hold 不可再判。

参考实现的窗口常量：Phira/prpr 用 `Perfect ±0.08 / Good ±0.16 / Bad ±0.22` 秒（Good 权重 0.65、Bad/Miss 权重 0 且断连）；phi-chart-render 用 `bad 180 ms / good 160 ms / perfect 80 ms`——两者在 Bad 窗口上不一致，`[未验证]` 哪个等于官方。

本项目渲染器的自动游玩策略（依项目说明）：**note 落到判定线上即视为被击中**，直接按 Perfect 计分并播放特效。

---

## 8. 官方引擎的行为怪癖（生成谱面时必须遵守，否则游戏会卡死或表现异常）

摘自 Lchzh Docs《实测数据》`[文档]`。**编辑器保存谱面时必须保证不产生下列情况**：

1. 任何一条判定线的事件列表**不能为空数组**（要么不写该字段，要么给出完整事件）。
2. 谱面运行时刻**不能超过任何一条事件列表所有事件的结束时刻**（因此每条事件列表末尾都要有「足够大 endTime」的哨兵事件）。
3. 判定线 `bpm` 不能为 0 或负数（否则谱面停顿）。
4. 判定线数目不要超过 100。
5. 速度事件列表第一条的 `startTime` 应为 0；其它事件列表第一条 `startTime` 应为 `-999999` 之类的极小值，最后一条 `endTime` 为 `1000000000` 之类的极大值。`[实测]` 本仓库官方样本完全符合（`-999999` / `1000000000`）。
6. 事件与上一事件相离时，会在空隙内做**解析延拓**；相交时相交部分被忽略（即 `startTime` 被截到上一事件的 `endTime`，并保持斜率与 `end` 不变地重算 `start`）。
7. `startTime >= endTime` 的事件会被忽略。
8. JSON 读取细节（官方为 float32 精度）：**重复键取第一个**（与 `JSON.parse` 的行为相反）；浮点为 float32 而非 double；数字支持科学计数法，格式见 `/^-?(0|[1-9]\d*)(\.\d+)?([Ee][+-]?\d+)?$/`；所有字段都有默认值（int=0、float=0.0、数组=[]、对象={}）；`int` 字段遇到小数会**截断**（`"3.9Music"` → 3）。
9. 速度事件 `value = 0` 表示判定线停止（note 冻结）；`value = 999` 是常见的「瞬移/无限远」手法 `[实测]`；`value < 0` 表示音符反向（向上飞），Hold 在尾部出现时整个音符会一起出现。`[文档]`
10. 性能：官方引擎读取 note 的时间复杂度是 **O(n²)**（65536 note 需要 6 s 打开），事件是 O(n)。编辑器与渲染器不要继承这个复杂度。

---

## 9. 存疑清单

### 9.1 已解决（原先存疑，现已确认）

| 问题 | 结论 | 依据 |
| --- | --- | --- |
| RPE 的速度单位是什么 | 1 = 每秒下落 120 个 RPE 长度单位（画布高 900 单位）= 2/15 屏幕高度/秒 ⇒ 官方 Y/s = RPE × 2/9 | Phichain《速度》+ 样本实测（`4495.5 = 999 × 4.5`） |
| `HL` 贴图何时使用 | 同一时刻存在 ≥ 2 个音符且开启「多押提示」 | sim-phi 参考实现 |
| 音符过线后多久消失 | 0.16 s 淡出 | sim-phi；prpr `FADEOUT_TIME = 0.16` 一致 |
| 判定线尺寸 | 6220.8 × 7.68 px（1920×1080 参考），缩放 `H/1080` | Lchzh 实测（5.76H）+ sim-phi 源码 |
| `floorPosition` 的定义 | 速度事件对**秒**的积分；与 note 自身 speed 无关 | Lchzh《相关计算》+ 本项目 280 个 note 逐一验证 |
| RPE 事件层的组合方式 | 相加（`a1(t) = a1(t) + a2(t)`） | Phira `Anim::chain` 注释 + 项目原始说明 |
| `numOfNotes` 是否含 Hold | RPE 的定义是「含假音符、**不含 Hold**」 | Phira 文档 + 样本实测（1252 = 1417 − 165） |
| 评分与物量口径 | `score = round((900000·Perfect + 585000·Good + 100000·maxCombo)/N)`；`N` = 非假音符数（**Drag 计入**） | PhiZone/player、sim-phi、Phira/prpr 三处实现一致 |
| 判定线 alpha 是否影响音符 | **不影响**（隐藏线时音符照常显示）；只有 RPE 负 alpha / 官方 PEC 负 alpha 编码例外 | 三个参考实现一致 + 本项目合成用例测试 |
| 父子判定线语义（**已定**） | 采用 **Phira 实现**：`pos_c = pos_p + R(rot_p)·偏移`；`rot_c = rot_c自身 + (rotateWithFather ? rot_p : 0)`，字段缺省视为 false；成环在 Phira 里直接报错，本项目降级为忽略+告警 | Phira 文档 + `prpr/src/core/line.rs`、`parse/rpe.rs` |
| 音符宽度（**已定**） | 作为**可修改的默认值**，默认 **W/8（0.125 画面宽）**（`N`/`M` 调整） | 项目决定；对照 prpr ≈0.132 W、phi-chart-render ≈0.118 W、参考效果图 ≈0.17 W（口径不同，不写死） |

### 9.2 仍待确认

- `[未验证]` 判定线长度的「官方」口径：Lchzh 实测 5.76H 与 sim-phi 源码一致；Phira 的 `lineLength` 默认 6.0（单位自述「待补充」）。建议以 5.76H 为基准并把长度做成配置项。
- `[未验证]` Phira 的 RPE 速度换算（`≈0.267`）比文档语义（`2/9 ≈ 0.222`）快约 20%，原因未明（可能与其画布单位有关）；若要与 Phira 逐帧对齐，需要用它的系数。
- `[未验证]` 项目原始说明中的判定线颜色 `(255,243,163)`（≈`#FFF3A3`）与长度 `3.6 × 屏幕宽度` 的来源：参考实现用的命中特效色是 `rgba(255,236,160,0.882)`（`#FFECA0`），判定线本体为 `#feffa9`；`3.6 W` @16:9 = 6.4H，也与实测 5.76H 不同。
- `[未验证]` RPE 各扩展事件（`textEvents`/`paintEvents`/`gifEvents` 等）的完整字段与版本差异：Phira 只实现了 `colorEvents / textEvents / scaleXEvents / scaleYEvents / inclineEvents / paintEvents / gifEvents`，其余（如 `textureEvents`）未见于实现。
- `[未验证]` Hold 打击特效循环间隔的官方口径（实测「与判定线 bpm 成反比」，bpm=30 → 1 s、bpm=60 → 0.5 s；参考实现用 `(now − lastHit) × holdTime ≥ 1.6e4 × holdSeconds`，与此一致）。
- `[未验证]` 官方 `formatVersion` 非 1/3 时的坐标规则（Lchzh 文档称以屏幕中心为原点、单位 0.1 H；Phira 直接报错不支持）。
- `[未验证]` 官谱是否已出现 `eventLayers` / `extended` 风格的新布局（本项目样本与 Phira 的官方解析器都只认扁平布局）。
- `[未验证]` 音符贴图的**官方**实宽：prpr 资源包 ≈0.132 画面宽、phi-chart-render 资源包 ≈0.118 画面宽（贴图 1000 px 宽、音符实宽约 942 px）、仓库参考效果图实测 ≈0.17 画面宽。渲染器默认取 **W/8** 并开放调整（`N`/`M` 或 `noteWidthRatio`）。
- `[未验证]` 「42 帧打击特效」只对本仓库 `assets/hit.png` 成立；参考实现把帧网格作为资源包参数（Phira `hitFx: [列,行]`，文档示例 [5,6]、默认包 [8,7]），未找到官方的帧数口径。
- `[未验证]` 第三方「统一格式」PhiCommonChart（<https://docs.nuanr-mxi.com/>）是否需要支持。
