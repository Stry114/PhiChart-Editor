## 说明
这份文档用于帮助接手此项目的编程者、AI迅速了解此项目。

Phigros是一款知名2D下落式音乐游戏。在游戏关卡中，存在多根判定线（line，默认为24根），会有音符（note）落到判定线上（从远处以靠近线，最终重合），玩家需要在音符与判定线重合时点击音符。

我们将关卡内容称为“谱面（chart）”，设计关卡称为“制谱”。

## 判定线
通常会有一或多根主判定线上有音符落下，其他判定线起表演作用。

判定线的中心位置、旋转角度、透明度、音符下落速度均会随时间变化，是通过事件event实现的。

## 文件
一个Phigros关卡存储为一个包（package），包内应至少包含：
 - 背景图（曲绘， illustration/background），是任意格式的图片。
 - 背景音乐（音频，audio/song），是任意格式的音频。
 - 谱面文件（谱面，chart），是一个json文件，通常非常大。
 - 除此之外，还可能包含一个txt文档用于记录关卡元数据，此项非必须，且格式未统一。
 - rpe格式允许自定义资源，也会一并放在包内根目录下。此项非必须。

谱面文件包含两种格式：official格式和rpe格式。二者采用了完全不同的文件结构、枚举类型编号和单位制。rpe格式在official格式上扩展了许多功能。

项目分别提供了两种格式的各一个包供调试。

包也可能以zip压缩包的形式提供，其内部结构应与上述一致。

## official json 格式简介
### 基本单位
 - 坐标系：经典平面几何数学坐标系。即：窗口左下角为原点，向上为y正，向右为x正。旋转角指的是与x轴正方向的夹角。
 - 距离单位X：1X=0.05625*窗口宽度
 - 距离单位Y：1Y=0.6*屏幕高度
### note 属性
 - time：时间。单位为1/32拍。与秒的换算关系为time=second*1.875/bpm。例如，bpm=120时，time=64对应 2拍 或 1秒。
 - positionX：决定音符落到线的哪个位置上，具体为沿着线的方向上，落点到线中心点的距离，单位为X。
 - type：音符分4种类型，1-4分别对应tap、drag、hold、flick。其中hold需要一直按住持续一段时间，引起其具有holdTime属性。
 - speed：下落速度倍率。
 - notesAbove/notesBelow：音符从线的哪一侧下落（靠近）。
 - floorPosition：未开始下落前，note距离判定线的距离，单位为Y

### event 属性
 - 事件定义了某一段时间内，判定线的某一个属性的线性变化。在事件时间内，属性值由初始值向结束值线性变化。
 - 事件通常连续，事件时间通常从0开始，结束于一个极大的值。若处于未定义区间内时，按照上一个事件的末值处理。处于首时间之前时，按默认值处理。
 - 事件数量通常巨大。需要一种方法，用于在给定特定时间后迅速计算出值。
 - move：线的中心点x/y坐标，单位为屏幕宽度/高度
 - speed：下落速度。
 - rotate：旋转角度，角度制。
 - disappear：不透明度（0~1）。
 - 应当参考以下文档：https://docs.lchzh.net/learning/phigros/calc


## rpe json 格式简介
相较官方增加了：
 - 完全不同的单位制、字段名。
 - 元数据、
 - 可变bpm、
 - 自定义材质贴图、
 - Control、
 - 每条线独立的bpm
 - 事件分层：类似图层叠加，各层的事件在某一刻的的值相加，即为此刻的最终值。
 - 事件缓动：允许以贝塞尔曲线或预设曲线的形式，实现非线性事件。speed事件不允许。
 - 父线：子线的坐标、旋转需要叠加上父线的值。
 - 扩展事件：允许修改线的粗细、长短、颜色等等。
 - 假note：不算分，不需点击，击中无特效，一样下落，仅供表演。
 - 可变大小的note
 - 可见时间：距离落到线上小于一定时间时，才渲染出来。

此外，以下功能用于制谱器，不影响游玩：
 - 判定线成组
 - 线名称

应当参考以下文档：https://docs.lchzh.net/learning/phiediter/


## 如何渲染游戏
 - 背景图缩放至铺满窗口，亮度降低至0.4倍，高斯模糊（半径约200-500）
 - 判定线为金色。宽度为0.006x窗口高度，长度为3.6x屏幕宽度。颜色为：(255, 243, 163)，透明度由事件控制。
 - tap、drag、flick为长条状，渲染时与线平行，逐渐靠近线（下落），重合后消失，重合时在落点播放击中动画。
 - hold有持续时间，相当于由厚度的tap，其长短与持续时间有关。其头部先接触判定线，并固定在判定线上，开始循环播放42帧击中动画，尾部继续向判定线靠近，直到尾部也抵达判定线，hold消失不再渲染，动画不再循环，播放结束后消失。
 - 击中动画（hit.png），共42帧，是灰度图，播放时着色为(255, 243, 163)，42帧按顺序播完，动画消失。
 - 右上角为分数，满分为1000000。每个note的分数为：1000000/note总数。
 - 左下角为歌名。
 - 右下角为难度。


## 我们要做什么
 - 游戏渲染器。渲染出关卡模拟，自动游玩关卡，用于预览最终效果。note落到线上后视为被玩家击中，并播放特效和计分。
 - 制谱器，用于设计谱面。内置实时渲染关卡的功能。UI风格参考Adobe Photoshop等，暗色背景，扁平图标，多彩、饱和度适当的颜色搭配。
 - 选用音频播放器时应注意：需要允许精准跳转到某一刻，需要允许暂停、播放，需要允许倍速播放。
 - 选用UI库时应注意：高性能，渲染器需要保持60-90帧，多线和note贴图的同屏半透明高性能渲染。制谱器需考虑同屏渲染数千个event的优化。未来，预留3D/伪3D改造的可能性。
 - 选用框架时注意：注重可分发性，为网页或其他便于多平台打包/编译，优先考虑Windows PC端。
